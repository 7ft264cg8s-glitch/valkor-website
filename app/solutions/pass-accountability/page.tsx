import { ContentPage } from "@/components/ContentPage";
import { passPage } from "@/lib/pageContent";

export default function PassAccountabilityPage() {
  return <ContentPage config={passPage} />;
}
